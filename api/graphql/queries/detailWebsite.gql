query detailWebsite($id: ID!) {
  websites(where: { id: $id }) {
    id
    url
    shortDescription
    targetAmount
    progressAmount
    monthlyProfit
    yearlyProfit
    numberOfInvestors
    description {
      html
    }
  }
}

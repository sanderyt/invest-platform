query detailWebsite($id: ID!) {
  websites(where: { id: $id }) {
    id
    url
    shortDescription
    slug
    targetAmount
    progressAmount
    monthlyProfit
    yearlyProfit
    numberOfInvestors
    description {
      html
    }
  }
}

mutation AddAccount(
  $firstName: String!
  $lastName: String!
  $residence: String!
  $email: String!
  $firebaseUid: String!
) {
  createAccount(
    data: {
      firstName: $firstName
      lastName: $lastName
      residence: $residence
      email: $email
      firebaseUid: $firebaseUid
    }
  ) {
    firstName
    lastName
    residence
    email
    firebaseUid
  }
}
